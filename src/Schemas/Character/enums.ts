export default `
    enum CharacterRole {
        MAIN
        SUPPORTING
        BACKGROUND
    }

    enum CharacterSort {
        ID
        ID_DESC
        ROLE
        ROLE_DESC
        SEARCH_MATCH
        FAVOURITES
        FAVOURITES_DESC
        RELEVANCE
    }
`;